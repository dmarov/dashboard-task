import { NgModule } from '@angular/core';

import {
    PaginationComponent,
} from '@/components';

@NgModule({
    declarations: [
        PaginationComponent,
    ],
})
export class PaginationModule { }
